const WorkoutListItem = ({ WorkoutListItem }) => {
    const { name } = WorkoutListItem;
    return (
        <div>
            <h2> {name} </h2>
        </div>
    )
}

export default WorkoutListItem